## 1. Audit-fix script

- [x] 1.1 Create `scripts/fix-viruddha-audit-recipes.js` with a `FIXES` table covering the 10 in-scope recipes (Chicken Biryani, Palak Paratha, Doodhi Chilla, Carrot Besan Chilla, Moong Dal Chilla, Turmeric Milk, Turmeric Milk with Dates, Tulsi Tea, Cinnamon Tea, Fennel Tea), dry-run by default, `--execute` to write; verified `node scripts/fix-viruddha-audit-recipes.js` (no flags) prints the planned change per recipe and makes zero DB writes
- [x] 1.2 Implement the Chicken Biryani fix: remove `Yogurt`, add `Coconut Milk` (50ml) to `ingredients`; verified via dry-run diff output showing the swap
- [x] 1.3 Implement the Palak Paratha fix: remove `Yogurt`, add `Water` (30ml) to `ingredients`; verified via dry-run diff output
- [x] 1.4 Implement the 3 chilla fixes (Doodhi Chilla, Carrot Besan Chilla, Moong Dal Chilla): leave `ingredients` untouched, prepend an `instructions` entry stating curd is served on the side, not mixed into the batter or heated; verified dry-run shows only an `instructions` change, no `ingredients` diff
- [x] 1.5 Implement the 5 honey-in-hot-liquid fixes (Turmeric Milk, Turmeric Milk with Dates, Tulsi Tea, Cinnamon Tea, Fennel Tea): leave `ingredients` untouched, prepend an `instructions` entry stating honey is added after the liquid cools slightly, off heat; verified dry-run shows only an `instructions` change
- [x] 1.6 Run `node scripts/fix-viruddha-audit-recipes.js --execute` against the dev database and verify it reports 10 recipes updated, 0 errors — done (updated=10, failed=0). Found and fixed a real bug in the process: the post-save `syncV1FromRecipe` hook is fire-and-forget and raced against `mongoose.disconnect()` for the last 2 recipes; script now explicitly awaits `syncV1FromRecipe` after every save (and after every skip, to backfill any prior race) before disconnecting.
- [x] 1.7 Verify idempotency: re-run `node scripts/fix-viruddha-audit-recipes.js --execute` and confirm it reports 0 further changes (already-fixed recipes detected and skipped) — done (updated=0, skipped=10, no sync errors)
- [x] 1.8 Spot-check one fixed `Recipe` via `Recipe.findOne` in a scratch script and confirm a new `RecipeVersion` was auto-created for it (post-save hook fired) without mutating any prior `RecipeVersion._id` — done: checked Chicken Biryani, Turmeric Milk, Palak Paratha, Fennel Tea. Each `RecipeVersion` v1 correctly reflects the fixed ingredients/instructions, upserted in place (no `PlanItem` references it yet in this dev DB, so in-place update is the correct/expected path, not a version bump). `hasUnresolvedIngredients: true` on all of them is pre-existing (this dev DB's `FoodItem` collection has 0 documents, confirmed separately) and affects every recipe equally, not something this change caused.

## 2. Slot-coverage script

- [x] 2.1 Create `scripts/add-slot-coverage-recipes.js` following `scripts/add-salad-recipes.js`'s structure (dry-run default, `--execute` to write, skip-if-name-exists), targeting dietician `tejasvini@docwellness.fit` — also added the same explicit `await syncV1FromRecipe(...)` fix as scripts/fix-viruddha-audit-recipes.js (same fire-and-forget-vs-disconnect race hit the last created recipe here too; fixed and backfilled)
- [x] 2.2 Populate the candidate list from design.md's Decisions section: 6 Brunch, 6 Night Drink, 6 non-Indian Lunch recipes, each with a `note` that explicitly tells the AI generator to avoid meat+dairy, heated curd/honey, and milk+sour-fruit combinations — 2 names adjusted from the original design draft for clarity/uniqueness (see design.md's updated Brunch list); recorded there
- [x] 2.3 Add the post-generation Viruddha keyword check (meat+curd/yogurt, milk+sour-fruit, curd/honey flagged as ingredients at all) that logs a non-blocking warning per recipe when triggered; verified in isolation (meat+curd, milk+sour-fruit, honey-only, and clean cases all matched expected output) and confirmed live: 3 of the 18 generated recipes (Chamomile Tea, Ashwagandha Milk, Licorice Tea) triggered a real non-blocking honey warning and were still created
- [x] 2.4 Run `node scripts/add-slot-coverage-recipes.js` (dry run) and verify it prints all 18 planned recipes with target slots and no DB writes or AI calls occur — done
- [x] 2.5 Run `node scripts/add-slot-coverage-recipes.js --execute` and verify it reports 18 created, 0 failed — done (18 created, 0 failed; 3 carried a logged Viruddha honey warning per 2.3)
- [x] 2.6 Re-run `node scripts/add-slot-coverage-recipes.js --execute` and verify it reports all 18 skipped (already exist), confirming idempotency — done (0 created, 0 failed, all 18 skipped, no AI calls)

## 3. Verification

- [x] 3.1 Re-export the full catalog with `node scripts/exportRecipesForAudit.js` and confirm: 10 fixed recipes show updated ingredients/instructions, Oats Porridge and Oats Pudding are unchanged, and slot counts for Brunch/Night Drink increased by 6 each — done. Catalog now 109 recipes (91+18). Brunch 7→13, Night Drink 8→14, Lunch 23→29. Oats Porridge/Oats Pudding ingredients unchanged. Chicken Biryani has Coconut Milk/no Yogurt, Palak Paratha has Water/no Yogurt.
- [x] 3.2 Run `openspec validate recipe-database-ayurveda-expansion --strict` and confirm it passes — done
