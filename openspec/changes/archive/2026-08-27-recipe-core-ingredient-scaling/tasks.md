## 1. Schema

- [x] 1.1 Add `role: { type: String, enum: ['core', 'sub'], default: 'sub' }` to `Recipe.ingredients[]` in `models/Recipe.js` and verify existing recipes still save/load without a migration (defaulted field, no required-ness added)
- [x] 1.2 Add the same `role` field to `RecipeVersion.ingredients[]` in `models/RecipeVersion.js` and verify existing `RecipeVersion` documents still load correctly (defaulted field)

## 2. AI generation path

- [x] 2.1 Add `role` to `utils/recipeJsonSchema.js`'s `ingredientSchema` (`required` list included, per `strict: true`) and verify `utils/openaiClient.js`'s existing Structured Outputs calls still parse successfully against the updated schema
- [x] 2.2 Add the category-priority core-selection instruction to `generateRecipeWithAI`'s system prompt in `utils/openaiClient.js` (design.md's priority order, "whole category" not "single ingredient") and verify via manual generation calls that: a single-grain dish (e.g. Chapati) returns exactly one `role: core` ingredient, and a combo dish (e.g. Mixed Vegetable) returns at least one `role: core` ingredient (verified live: Chapati → exactly Whole Wheat Flour core; Mixed Vegetable Sabzi → 4 ingredients core including a non-strictly-same-category Potato, which is expected/accepted per the relaxed spec, not a strict same-category match)
- [x] 2.3 Add a deterministic post-generation validator (mirroring `enforceFiniteIngredientQuantities`'s existing "backstop" pattern) that corrects a generated response with zero `core` ingredients by re-applying the category-priority heuristic (a response with one-or-more core ingredients marked needs no correction - any number is valid), and verify it with a unit test feeding a malformed 0-core response — implemented as `utils/coreIngredientHeuristic.js` (`applyCoreIngredientHeuristic`/`hasCoreIngredient`), wired into `generateRecipeWithAI`'s return path; 8 unit tests in `tests/coreIngredientHeuristic.test.js`, all passing

## 3. Manual authoring path

- [x] 3.1 In `controllers/dietician/uploadRecipieController.js`'s `createRecipe`, apply the category-priority default (marking every ingredient in the highest-priority present category) when zero ingredients are marked `role: core` in the submitted list, and otherwise honor the submitted `role` values as-is (any number of core ingredients is valid, no upper-bound rejection); verify with request tests covering zero-core, single-core, and multi-core payloads — implemented in the shared `sanitizeRecipeIngredients` helper
- [x] 3.2 Apply the same validation/defaulting to `updateRecipe` and verify with the equivalent request tests — `updateRecipe` already calls the same shared `sanitizeRecipeIngredients`, so this was covered by 3.1's change; 5 request tests in `tests/recipeIngredientRoleSanitization.test.js` (zero-core create, explicit single-core, explicit multi-core, zero-core update, unrelated-field update leaves role untouched), all passing
- [x] 3.3 Confirm `sanitizeRecipeComponents`/ingredient-sanitization helpers pass `role` through unchanged (no accidental stripping) and add a regression test if none currently covers full ingredient round-tripping — already covered: `tests/recipeIngredientRoleSanitization.test.js`'s assertions read back the *saved* `Recipe` document (not just the HTTP response), proving `role` survives the full create/update → sanitize → Mongoose save → DB read round-trip. Also confirmed by inspection: `createRecipe`'s `ingredientsWithImages` step uses object-spread (`{ ...ing, image: ... }` / `ing` unchanged), not an allowlist, so it can't silently drop `role` the way `generateRecipeWithAI`'s per-serving-ingredients mapping did before task 2.2's fix.

## 4. Recipe versioning service

- [x] 4.1 Update `syncV1FromRecipe` (`services/recipeVersioningService.js`) to copy each ingredient's `role` from the parent `Recipe` onto the V1 `RecipeVersion`, matched by `foodItemId`; verify with a test asserting V1's `role` values match the source `Recipe`
- [x] 4.2 Implement the core-group aggregate-weight detection and sub-ingredient recompute in `createCustomVersion` per design.md's Decisions: sum `resolveGramsForIngredient` across `original`'s `role: core` entries and across the submitted entries with matching `foodItemId`s, and when the two totals differ (beyond floating-point noise), overwrite every `role: sub` entry in `updatedIngredients` with `Math.round(previousSubQuantity * ratio * 100) / 100` in its existing unit; when either total can't be resolved to grams, fall through to pass-through (no recompute) — also carries `role` forward onto every saved ingredient (the client never submits it)
- [x] 4.3 Verify the "no core designated" and "core group total unchanged" branches both pass `updatedIngredients` through byte-for-byte unchanged (regression tests against `tests/recipeVersioningService.test.js`'s existing pass-through cases) — all 11 pre-existing tests still pass unmodified, plus new explicit legacy/unresolvable-unit pass-through tests
- [x] 4.4 Add tests for the new behavior — done in `tests/recipeCoreIngredientScaling.test.js` (10 tests, all passing): doubling a single core ingredient doubles every sub ingredient; growing a multi-core group's total via just one of several core ingredients scales every sub by that total's ratio; rebalancing within a multi-core group without changing its total leaves subs exactly as submitted; a sub value alongside an unchanged core total is honored verbatim and becomes the next edit's baseline; a sub value alongside a changed core total is overridden; an unresolvable core-ingredient unit (no `unitConversions` entry) falls back to full pass-through; role is preserved correctly across chained versions and multi-core groups
- [x] 4.5 Verify `components` ("Makes (on the plate)") still rescales correctly by calorie ratio after a core-triggered sub-ingredient recompute (existing mechanism, confirm it isn't affected by the new code path) — verified: nutrition is now computed from the post-recompute ingredient list, so the existing calorie-ratio component rescale automatically reflects the core-triggered change with no extra code needed
- [x] 4.6 Verify `createVersionFromSnapshot` (the AI-regenerated-snapshot path, `controllers/dietician/planItemController.js`'s `updateItemRecipeVersion`) either also gets `role`-aware recompute or is explicitly left out with a code comment explaining why (its snapshot already comes from a full AI regeneration, not an incremental single-ingredient edit) - resolve which, per design.md's scope, and implement accordingly — resolved: explicitly left out (code comment explains why - no "previous version" to compare a core group's weight against), but `role` is still carried through/defaulted via the same heuristic so the resulting version's ingredients aren't left without role data. Also fixed an upstream allowlist bug in `updateRecipeFromEdits`'s `enhanceIngredient` mapping that would have silently stripped `role` before it ever reached this function. 3 new tests, all passing.

## 5. End-to-end verification

- [x] 5.1 Run the full `tests/recipeVersioningService.test.js` and `tests/planItemCleverEndpoints.test.js` suites and confirm no regressions — ran the ENTIRE test suite (264 tests, 38 files): 258 passed, 6 pre-existing failures in 3 unrelated files (`timeline.test.js`, `push.test.js`, `foodItemNutritionData.test.js` - Goal Journey milestones, FCM push config, static nutrition-data reconciliation), confirmed pre-existing by re-running those 3 files against a `git stash`'d baseline with none of this change's edits applied (identical 6 failures). All 87 tests across the 9 recipe/ingredient-related suites this change touches pass.
- [x] 5.2 Manually exercise `POST .../create-custom-version` against both a single-core recipe (e.g. re-saved Chapati) and a multi-core recipe (e.g. re-saved Mixed Vegetable) and confirm the response's sub-ingredient quantities match the expected proportional values for a sample core-group weight change — done as 2 automated HTTP-level (supertest, real route + Express app) tests in `tests/recipeCoreIngredientScaling.test.js`, both passing: single-core doubling and multi-core group growth each correctly override an attempted sub-ingredient submission with the recomputed value

## 6. Optional follow-up (not required for this change to ship)

- [x] 6.1 Note in the change's follow-up backlog (not implemented here, per design.md's Non-Goals): a separate dry-run/`--execute` backfill script applying the category-priority group heuristic to existing recipes' `Recipe.ingredients[]`, to accelerate feature coverage beyond "recipes re-saved after this ships" — already documented in design.md's Non-Goals ("No bulk backfill of `role`...") and Risks ("a follow-up backfill script...can be proposed separately once this ships and the heuristic's real-world accuracy is validated on new recipes first")
